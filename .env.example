# Database Configuration
# For development - local PostgreSQL
DATABASE_URL=postgresql://username:password@localhost/dbname

# For production - ElephantSQL example
# DATABASE_URL=postgresql://username:password@raja.db.elephantsql.com/database_name

# For production - AWS RDS example  
# DATABASE_URL=postgresql://username:password@your-rds-endpoint.amazonaws.com:5432/dbname

# Application Settings
PROJECT_NAME=Blood Bank API
PROJECT_DESCRIPTION=Blood Bank Management System API
VERSION=1.0.0
API_PREFIX=/api/v1
DOCS_URL=/docs

# Security Settings
SECRET_KEY=your-super-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings - Update with your actual frontend URLs
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:3001", "https://yourdomain.com"]

# Email Configuration (if you use email features)
# MAIL_USERNAME=your-email@example.com
# MAIL_PASSWORD=your-email-password
# MAIL_FROM=your-email@example.com
# MAIL_PORT=587
# MAIL_SERVER=smtp.gmail.com
# MAIL_FROM_NAME=Blood Bank System

# Redis Configuration (if you use caching)
# REDIS_URL=redis://localhost:6379

# AWS Lambda (automatically set in Lambda environment)
# AWS_LAMBDA_FUNCTION_NAME=your-function-name

# Environment
ENVIRONMENT=development